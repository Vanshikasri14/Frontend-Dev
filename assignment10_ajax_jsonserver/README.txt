ASSIGNMENT 10 - JSON Server + AJAX / Fetch / Axios

Each question is in its own folder with a separate db.json file and front-end files.

Ports used (you can change if needed):
- Q1: 3001
- Q2: 3002
- Q3: 3003
- Q4: 3004
- Q5: 3005
- Q6: 3006

Example commands (run from each question folder):

Q1:
  json-server --watch db.json --port 3001

Q2:
  json-server --watch db.json --port 3002

Q3:
  json-server --watch db.json --port 3003

Q4:
  json-server --watch db.json --port 3004

Q5:
  json-server --watch db.json --port 3005

Q6:
  json-server --watch db.json --port 3006

Then open the corresponding index.html file in your browser (using Live Server in VS Code or just opening the file).
